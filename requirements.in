# BluBot Source Dependencies (v3.8.2)
# Use 'pip-compile requirements.in' to generate requirements.txt

atproto>=0.0.67
google-genai>=2.8.0
feedparser>=6.0.12
python-dotenv>=1.2.1
requests==2.34.2
Mastodon.py>=2.2.1
beautifulsoup4>=4.15.0
httpx>=0.28.0
Pillow==12.2.0

# Security Hardening (Specific Transitive Pins)
# SECURITY-NOTE: cryptography is currently constrained by atproto's dependency cap (<47).
# The fixed cryptography version is >=48.0.1. Dependabot cannot resolve the upgrade
# until atproto relaxes its cryptography constraint. We are accepting the risk temporarily
# because the bot runs as a short-lived GitHub Actions job and is not a persistent
# public web service. Revisit this when atproto releases a compatible version.
cryptography>=46.0.7,<47
urllib3==2.7.0
anyio==4.13.0
zipp==4.1.0
dnspython==2.8.0

# Testing
pytest>=8.0.0
pytest-asyncio>=1.4.0
pytest-mock>=3.12.0
